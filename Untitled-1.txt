{
  "terminal.integrated.cwd": "${workspaceFolder}"
}
``` تنظیم باعث می‌شود که ترمینال در مسیر پروژه‌ی فعلی شما باز شود. متغیر `${workspaceFolder}` به صورت خودکار با مسیر پروژه‌ی فعلی شما جایگزین می‌شود.

اگر می‌خواهید مطمئن شوید که از پاورشل به عنوان ترمینال پیش‌فرض استفاده می‌شود، می‌توانید این تنظیم را هم اضافه کنید:
